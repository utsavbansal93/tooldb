# GitHub Personal Access Token — enables L3 discovery (GitHub search API)
# Create at https://github.com/settings/tokens (no special scopes needed)
GITHUB_TOKEN=

# Brave Search API Key — enables L4 discovery (web search fallback)
# Get one at https://brave.com/search/api/
BRAVE_API_KEY=
